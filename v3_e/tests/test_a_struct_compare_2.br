struct Car {
    model: string;
    year: int;
}

func main() : void {
    var car: Car;

    print(car == nil);

    car = new Car;
    print(car == nil);
    print(car != nil);
}

/*
*OUT*
true
false
true
*OUT*
*/